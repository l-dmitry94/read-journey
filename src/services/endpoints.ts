const ENDPOINTS = {
    auth: {
        signup: '/users/signup',
        signin: '/users/signin',
        current: '/users/current',
        signout: '/users/signout',
    },
};

export default ENDPOINTS;
